# 1. Написать программу, которая будет складывать, вычитать, умножать или делить два числа.
# Числа и знак операции вводятся пользователем.
# После выполнения вычисления программа не завершается, а запрашивает новые данные для вычислений.
# Завершение программы должно выполняться при вводе символа '0' в качестве знака операции.
# Если пользователь вводит неверный знак (не '0', '+', '-', '*', '/'), программа должна сообщать об
# ошибке и снова запрашивать знак операции. Также она должна сообщать пользователю о
# невозможности деления на ноль, если он ввел его в качестве делителя.

command = ""
while command != "0":

    command = input("Введите команду ")

    if command != "0" and command != "*" and command != "/" and command != "+" and command != "-":
        print("Неверная команда")
        continue

    if command == "0":
        continue

    a, b = int(input("a ")), int(input("b "))

    if command == "/" and b == 0:
        print("Неверные аргументы")
        continue

    if command == "*":
        print(f"Произведение {a*b}")
    elif command == "/":
        print(f"Часное {a / b}")
    elif command == "+":
        print(f"Сумма {a + b}")
    elif command == "-":
        print(f"Разность {a - b}")

